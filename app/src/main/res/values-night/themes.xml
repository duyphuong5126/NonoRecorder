<resources>
    <!-- Base application theme. -->
    <style name="Theme.NonoRecorder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_container</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_container</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NonoRecorder.Player" parent="Theme.NonoRecorder">
        <item name="playerDefaultCoverTint">@color/gray_500</item>
        <item name="playerControlComponentColor">@color/white</item>
        <item name="backgroundColor">@color/black</item>
        <item name="buffered_color">@color/gray_600</item>
        <item name="played_color">@color/white</item>
        <item name="scrubber_drawable">@drawable/ic_circle_solid_white_24dp</item>
        <item name="unplayed_color">@color/gray_200</item>
    </style>
</resources>